Flask==2.3.3

git init

# Add files to the repository
git add app.py requirements.txt

# Commit the files
git commit -m "Initial commit: Flask app with Hello World and personalized greeting"

# Link to github
git remote add origin https://github.com/<your-username>/flask-hello-world.git

# Push to GitHub
git branch -M main
git push -u origin main
